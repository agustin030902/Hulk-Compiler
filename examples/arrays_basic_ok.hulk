// Arreglos: literal, indexación, mutación y size().
let a: Number[] = {10, 20, 30} in {
    print(a[0]);        // 10
    print(a[1] + a[2]); // 50
    a[1] := 99;
    print(a[1]);        // 99
    print(a.size());    // 3
};
